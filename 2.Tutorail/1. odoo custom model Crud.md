Odoo তে simple CRUD module বানানো খুব easy, কিন্তু শুরুটা cleanভাবে করলে পরে upgrade-safe থাকবে।

নিচে **step-by-step minimal CRUD module** দেওয়া হলো (Odoo 19 compatible) 👇

---

# 1. Custom addons folder

```bash
mkdir -p ~/Revision-Codes/Odoo19/practise_1/odoo/custom_addons
```

👉 তারপর `odoo/odoo.conf` এ add করো:

```ini
addons_path = /home/ashraful/Revision-Codes/Odoo19/practise_1/odoo/addons,
             /home/ashraful/Revision-Codes/Odoo19/practise_1/odoo/odoo/addons,
             /home/ashraful/Revision-Codes/Odoo19/practise_1/odoo/custom_addons
```

---/home/ashraful/Revision-Codes/Odoo19/practise_1/Tutorials/odoo Custom model Crud.txt

# 2. Module generate করো

```bash
cd ~/Revision-Codes/Odoo19/practise_1/odoo
python odoo-bin scaffold student_management custom_addons
```

👉 এতে folder তৈরি হবে:

```text
custom_addons/student_management/
```

---

# 3. Model (CRUD core)

👉 `models/models.py`:

```python
from odoo import models, fields

class Student(models.Model):
    _name = 'student_management.student_management'
    _description = 'Student Info'

    name = fields.Char(string="Name", required=True)
    age = fields.Integer(string="Age")
    email = fields.Char(string="Email")
```

---

# 4. Init files update

👉 `models/__init__.py`:

```python
from . import models
```

👉 root `__init__.py`:

```python
from . import models
```

---

# 5. View (UI for CRUD)

👉 `views/student_view.xml`:

```xml
<odoo>
    <record id="view_student_tree" model="ir.ui.view">
        <field name="name">student.tree</field>
        <field name="model">student_management.student_management</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="age"/>
                <field name="email"/>
            </list>
        </field>
    </record>

    <record id="view_student_form" model="ir.ui.view">
        <field name="name">student.form</field>
        <field name="model">student_management.student_management</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="age"/>
                        <field name="email"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_student" model="ir.actions.act_window">
        <field name="name">Students</field>
        <field name="res_model">student_management.student_management</field>
        <field name="view_mode">list,form</field>
    </record>

    <menuitem id="menu_student_root" name="Student"/>
    <menuitem id="menu_student" name="Students" parent="menu_student_root" action="action_student"/>
</odoo>
```

⚠️ **Odoo 19 Note:** `<tree>` এর জায়গায় `<list>` use করো। `view_mode="tree,form"` এর জায়গায় `view_mode="list,form"` use করো।

---

# 6. Manifest update

👉 `__manifest__.py`:

```python
{
    'name': 'Student Management',
    'version': '1.0',
    'depends': ['base'],
    'data': [
        'views/student_view.xml',
    ],
    'installable': True,
    'application': True,
}
```

---

# 7. Server restart + install

```bash
cd ~/Revision-Codes/Odoo19/practise_1/odoo
python odoo-bin -c odoo.conf -u student_management --stop-after-init
```

👉 অথবা UI থেকে:

* Apps → Update Apps List → Install

---

# 8. CRUD Ready

এখন তুমি পারবে:

* Create
* Read
* Update
* Delete

---

# BONUS: REST API (HTTP)

👉 controller add করো:

`controllers/controllers.py`

```python
from odoo import http
from odoo.http import request, Response
import json

class StudentAPI(http.Controller):

    @http.route('/api/students', type='http', auth='public', methods=['GET'], csrf=False)
    def get_students(self):
        students = request.env['student_management.student_management'].search([])
        data = [{
            'id': s.id,
            'name': s.name,
            'age': s.age,
            'email': s.email
        } for s in students]
        return Response(json.dumps(data), content_type='application/json')
```

👉 `controllers/__init__.py`:

```python
from . import controllers
```

👉 root `__init__.py` এ add করো:

```python
from . import models
from . import controllers
```

⚠️ **Odoo 19 Note:** `type='json'` এর জায়গায় `type='http'` use করো, আর JSON response এর জন্য `Response(json.dumps(data), content_type='application/json')` return করো।

---

# Important Tips

* direct DB query কোরো না
* ORM use করো
* future upgrade safe থাকবে

---

# Next Step

তুমি এখন চাইলে:

* relation (Many2one, One2many)
* pagination API
* authentication (JWT)
* Next.js integration

👉 এগুলো add করতে পারো
