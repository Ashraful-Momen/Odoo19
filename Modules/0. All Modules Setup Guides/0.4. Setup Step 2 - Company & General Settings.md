# Setup Step 2 : Company & General Settings — প্রতিটা Option Explain

**Series: Odoo 19 Accounting Perfect Setup (Step 2 of 6)**

> **Company Setup মানে শুধু নাম-ঠিকানা দেওয়া না। এখানের প্রতিটা Field পরে Invoice, Tax Report, Legal Document—সব জায়গায় যায়। একটা ভুল = শত শত Document-এ ভুল।**

---

# কোথায় যাবে

```
Settings

↓

Users & Companies

↓

Companies

↓

তোমার Company-তে Click
```

---

# প্রতিটা Field — কী / কেন / না করলে কী হয়

## 1. Company Name

```
কী:        তোমার Company-র Legal নাম

কেন:       প্রতিটা Invoice, Bill, Report-এর

          উপরে এই নাম ছাপবে
```

```
করলে:      সব Document Professional দেখাবে

না করলে:   Invoice-এ "YourCompany" লেখা যাবে

          → Client-এর চোখে অপ্রফেশনাল

          → Tax Authority-র কাছে অবৈধ Document
```

> **⚠️ Trade License/TIN-এ যে নাম, হুবহু সেই নাম দাও। Short নাম দিলে Legal Document মিলবে না।**

---

## 2. Address (Street, City, Zip, Country)

```
কী:        Company-র ঠিকানা

কেন:       ৩টা জায়গায় লাগে—

          ১. Invoice PDF-এ ছাপে

          ২. Tax Report-এ যায়

          ৩. Country দেখে Odoo-র

             অনেক Behavior ঠিক হয়
```

```
Country সঠিক না দিলে:

   ❌ ভুল Tax Rule চলতে পারে

   ❌ ভুল Report Format আসতে পারে

   ❌ Currency-তে সমস্যা
```

---

## 3. VAT / Tax ID (TIN)

```
কী:        সরকারের দেওয়া Tax নম্বর

          (বাংলাদেশে: BIN/TIN)
```

```
করলে কী হয়:

   ✅ প্রতিটা Invoice-এ Auto ছাপে

   ✅ Tax Return-এর সময় Ready থাকো

না করলে কী হয়:

   ❌ Customer তোমার Invoice দিয়ে

      VAT Adjustment নিতে পারবে না

      → তারা ভুগবে, তোমার Image নষ্ট

   ❌ Audit-এ জরিমানা
```

> **এটা Setup-এর সবচেয়ে গুরুত্বপূর্ণ ৩টা Field-এর একটা। এখনই দাও।**

---

## 4. Currency

```
কী:        Company-র মূল মুদ্রা

          (সব Account-এর Balance এই মুদ্রায়)
```

```
করলে:      সব Report সঠিক মুদ্রায় আসে

ভুল দিলে কী হয়:

   ❌ প্রথম Entry Post হওয়ার পর

      Currency বদলানো প্রায় অসম্ভব

   ❌ সব Balance ভুল মুদ্রায় জমা হয়ে যাবে

   ❌ শেষ সমাধান: নতুন Database
```

> **🚨 RED ALERT: প্রথম Transaction-এর আগে এই Field দুইবার Check করো। এটাই Setup-এর সবচেয়ে ভয়ংকর ভুল।**

---

## 5. Company Logo

```
কী:        Invoice/Report-এর উপরে যে Logo ছাপে
```

```
করলে:      Client-এর কাছে Brand-এর প্রতি আস্থা বাড়ে

না করলে:   Invoice-এ ফাঁকা জায়গা / Default Logo

          → ছোট Company ভাববে
```

```
Tips: পরিষ্কার PNG, সাদা Background, 300 DPI মতো
```

---

## 6. Email / Phone / Website

```
কী:        Invoice-এর Footer-এ Contact হিসেবে যায়
```

```
করলে:      Client প্রশ্ন থাকলে সরাসরি যোগাযোগ করে

          → Payment দ্রুত আসে

না করলে:   Client উত্তর খুঁজে পায় না

          → Payment আটকে যায়
```

---

## 7. Company Tagline

```
কী:        নামের নিচে এক লাইন

          যেমন: "Enterprise Software Solutions"
```

```
Optional — থাকলে ভালো দেখায়, না থাকলে সমস্যা নেই
```

---

# Document Layout — Invoice-এর চেহারা

```
Settings

↓

General Settings

↓

Companies Section

↓

Configure Document Layout
```

## এখানে কী কী Option

```
Layout Template (কয়েকটা Ready Design)

   → কোনটা Select করলে Invoice সেভাবে দেখাবে
```

```
Colors (Primary/Secondary)

   → তোমার Brand Color দাও

   → Invoice তোমার Brand-এর মতো দেখাবে
```

```
Font

   → Professional Font Select করো
```

```
Company Tagline / Footer

   → প্রতিটা Page-এর নিচে কী থাকবে
```

```
করলে:      Client PDF খুললেই বোঝে — প্রফেশনাল Company

না করলে:   Default Layout — সবার মতোই, আলাদা কিছু না
```

> **Preview Button চেপে একটা Sample Invoice দেখে নাও। ৫ মিনিটের কাজ, প্রথম Impression চিরকালের।**

---

# Fiscal Year (Company Settings-এ)

```
Accounting

↓

Configuration

↓

Settings

↓

Fiscal Year
```

## Options

```
Last Day of Fiscal Year    → যেমন: 30 June

Last Month of Fiscal Year  → যেমন: June
```

```
করলে কী হয়:

   ✅ Report সঠিক Period-ে আসে

   ✅ বছর শেষে Odoo সঠিক দিনে Closing করে

   ✅ Tax Period মিলে যায় সরকারের সাথে
```

```
না করলে / ভুল করলে:

   ❌ জুলাই–জুন Company-র Report

      জানুয়ারি–ডিসেম্বর ধরে আসবে

   ❌ সরকারের Tax Period-এর সাথে মিলবে না

   ❌ Year-End Closing গুলিয়ে যাবে
```

> **বাংলাদেশে অধিকাংশ Company: জুলাই–জুন। তোমার Tax Advisor-কে জিজ্ঞেস করে নিশ্চিত হয়ে দাও।**

---

# Multi Company চালু করবে?

```
Settings → General Settings → Multi Company
```

```
একটাই Company হলে:

   → চালু করো না

   → চালু করলে অদরকারি Company Selector দেখাবে,

     ভুল করে Entry অন্য Company-তে যেতে পারে
```

```
একাধিক Company হলে:

   → চালু করো

   → তারপর প্রতিটা Company আলাদা Setup

     (Module 14: Multi Company মনে আছে?)
```

---

# Developer Perspective

```
সব Company Data → res.company

   name, vat, currency_id, country_id,

   logo (binary), fiscalyear_last_day,

   fiscalyear_last_month
```

```
Document Layout → report layout template

   (web.layout / external_layout)
```

> **Custom Report বানালে Company Field-গুলো এখান থেকেই Pull হয়। Setup পরিষ্কার থাকলে Report-এ কিছু Hardcode করতে হয় না।**

---

# Common Mistakes

❌ Currency ভুল দেওয়া — Setup-এর সবচেয়ে ভয়ংকর ভুল।

❌ Trade License-এর নামের বদলে অন্য নাম দেওয়া।

❌ VAT Number পরে দেব ভেবে রেখে দেওয়া — প্রথম Invoice-এই লাগবে।

❌ Fiscal Year এলোমেলো রাখা — Tax Period গুলায়।

❌ এক Company-তেও Multi Company চালু করা।

❌ Logo-তে বড়/অস্পষ্ট File দেওয়া — Invoice PDF ভারী হয়।

---

# Checklist

```
□  Legal Name (License অনুযায়ী)

□  পুরো Address + সঠিক Country

□  VAT/Tax ID Number

□  Currency দুইবার Check ✓✓

□  Logo (পরিষ্কার, হালকা File)

□  Email + Phone + Website

□  Document Layout Configure + Preview দেখেছি

□  Fiscal Year Start/End ঠিক আছে

□  Multi Company — শুধু লাগলেই চালু
```

---

# পরের Step

```
Setup Step 3 → Accounting Settings A to Z

(0.8. নম্বর File — একই Folder-এ)
```

Company Ready। এবার Accounting-এর ভিতরের প্রতিটা Switch চালু/বন্ধ করবে — জেনে, বুঝে।
